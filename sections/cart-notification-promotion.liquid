{%- comment -%}
  Shirt bundle promotion for the add-to-cart notification.

  Rendered through the Section Rendering API — see getSectionsToRender in
  assets/cart-notification.js — so the message is computed from the committed
  cart on the server. The markup and the shirt count both live in
  snippets/shirt-promotion.liquid, shared with the cart page and the drawer.
{%- endcomment -%}

{%- render 'shirt-promotion' -%}

{% schema %}
{
  "name": "Cart promotion"
}
{% endschema %}
